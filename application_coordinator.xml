<coordinator-app name="Application_Coordinator" frequency="${coordinatorFrequency}" start="${startTime}" end="${endTime}" timezone="${timeZone}" xmlns="uri:oozie:coordinator:0.1">
   <controls>
      <timeout>${timeOut}</timeout>
   </controls>
   <action>
      <workflow>
         <app-path>${workflowPath}</app-path>
         <configuration>
            <property>
               <name>jobTracker</name>
               <value>${jobTracker}</value>
            </property>
            <property>
               <name>nameNode</name>
               <value>${nameNode}</value>
            </property>
            <property>
               <name>queueName</name>
               <value>${queueName}</value>
            </property>
            <property>
               <name>masterNode</name>
               <value>${masterNode}</value>
            </property>
            <property>
               <name>appName</name>
               <value>${appName}</value>
            </property>
            <property>
               <name>appClass</name>
               <value>${appClass}</value>
            </property>
            <property>
               <name>maxSleepTime</name>
               <value>${maxSleepTime}</value>
            </property>
         </configuration>
      </workflow>
   </action>
</coordinator-app>
